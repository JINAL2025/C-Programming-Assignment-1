#include <stdio.h>
void main()
{
int h, l, a;
printf("Enter the height and lenghth of base of a triangle : ");
scanf("%d, %d", &h, &l);
a=(h*l)/2;
printf("The area of the triangle is %d sq. units\n", a);
}